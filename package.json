{
  "name": "testapiui",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
 "scripts": {
"clean:reports": "rimraf allure-report allure-results",
  "test:kib": "cucumber-js -p kib",
  "test:flutter": "cucumber-js -p flutter",
  "test:api": "cucumber-js -p api",
  "test:all": "npm run clean:reports && npm run test:kib & npm run test:flutter & npm run test:api & npm run generate:report && npm run open:report",
  "generate:report": "allure generate allure-results --clean -o allure-report",
  "open:report": "allure open allure-report",
  "test": "npm run test:all"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@cucumber/cucumber": "^12.6.0",
    "@playwright/test": "^1.58.2",
    "@types/node": "^25.2.1",
    "allure-cucumberjs": "^3.4.5",
    "allure-playwright": "^3.4.5",
    "rimraf": "^6.1.2",
    "ts-node": "^10.9.2"
  }
}